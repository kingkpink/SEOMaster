[project]
name = "hosted-skill-api"
version = "0.1.0"
description = "FastAPI hosted SEO skill API"
readme = "README.md"
# Use a stable 3.14.x interpreter (e.g. 3.14.0+). Pip enforces this when installing this dir: pip install .
requires-python = ">=3.14,<3.15"

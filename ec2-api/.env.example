# Copy to .env on the server (never commit .env)
ANTHROPIC_API_KEY=sk-ant-api03-...
# Comma-separated keys issued to customers (simple shared-secret auth)
API_KEYS=your-first-key,another-customer-key
# Optional: override model
ANTHROPIC_MODEL=claude-sonnet-4-20250514
# Public URL — used by GET /install to generate the CLI script
PUBLIC_URL=https://awsec2.tail0fdcca.ts.net
